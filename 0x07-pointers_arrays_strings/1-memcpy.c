#include "main.h"
/**
*_memcpy - copies memory area
*@src: memory area
*@dest: pointer
*@n: bytes
*Return: Always 0 (Success)
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int n

	for (i = 0; i < n; i++);
	s[i] = dest;
	return (dest)
}
