#include "stdio.h"
/**
*print_array - prints n element of an array of integers
*@str: the input array
*@n: the largest of the array
*/
void print _array(int *a, int n)
{
	int index_of_array;
	{
		printf("%d", a[index_of_array]);
		if (index_of_array ! = [n - 1])
		{
			printf(",");
		}
				putchar('\n')
	}
}
